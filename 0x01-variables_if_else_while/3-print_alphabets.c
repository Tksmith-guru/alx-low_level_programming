#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char alphabets;
	char CAPS;

	for (alphabets = 'a'; alphabets <= 'z'; alphabets++)
		putchar(alphabets);
	for (CAPS = 'A' CAPS <= 'z'; CAPS++)
		Putchar(CAPS);
	Putchar('\n');
	return (0);
}
